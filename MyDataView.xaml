<Window x:Class="Scratch.MyDataView"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008" 
             mc:Ignorable="d" 
             d:DesignHeight="300" d:DesignWidth="300">
    <StackPanel>
        <WrapPanel>
            <Label Content="The Value:" />
            <TextBox Text="{Binding Path=TheValue, Mode=TwoWay}" Width="200" />
        </WrapPanel>
        <WrapPanel HorizontalAlignment="Right">
            <Button Name="DoSomethingCustom" Content="Custom Binding" />
            <Button Name="DoAnotherThing" Content="Custom Binding with a parameter" />
            <Button Name="Deactivate" Content="Deactivate" />
            <Button Name="Close" Content="Close" />
            <Button Name="Shutdown" Content="Shutdown" />
        </WrapPanel>
    </StackPanel>
</Window>
